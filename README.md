# GDI Starter

## Getting starting

To launch project, install deps by running `npm ci` in root folder and then use `npm start`.

## Production mode

You can also run project in production mode with cache.\
For this you need to make a build first `npm run build`.\
Then just use `npm run prod`.
